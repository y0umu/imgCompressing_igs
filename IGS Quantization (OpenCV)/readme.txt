这是IGS量化方法的OpenCV实现。使用C++。源代码在imgIgs.cpp中。
因为使用了OpenCV的动态链接库opencv_world320d.dll，使用的时候务必保证程序和库放在
同一个目录里。如果不在同一个目录，至少也请保证opencv_world320d.dll在PATH中。

不知道什么是PATH的话，请上网搜索一下。

要是谁有幸找到这个地方会不会对图像处理产生一些兴趣呢（笑）。